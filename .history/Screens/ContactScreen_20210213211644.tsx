import * as React from 'react';
import {
    View,
    Text,
    StyleSheet,
    Image,
    TouchableOpacity,
    FlatList,
    Platform,
    SafeAreaView,
  } from "react-native";

export default class ContactScreen extends React.Component {
    render() {
        render(

        )
    }
}